objetivo: incorporar el cambio de tempo, y hacer un scrol mas smooth
	para el siguiente commit he de traer el codigo de la rama del cursor, en donde este el bpm array etc...

	decido prescindir de la animation ya q vuelve al principio cada vez q se reinicia(recuerdese que para cambiar el tempo de la animacion se tiene q reiniciar esta)
	así q encuentro la distancia de cada compas mediante

	bars = document.getElementsByClassName('bar');
	for (var i = 0; i < bars.length; i++) {
		// console.log("bars[i].nextSibling[x] : " + bars[i].nextSibling["x"]);
		console.log(" bars[i].nextSibling.innerHTML : " +  bars[i].nextSibling.getAttribute("x"));

	tab hago lo de tener en cuenta la cantidad de bemoles etc a la hora de colocar el marcadorPosicion(antiguo cursor)
		en el colors.js
		try{
			posicionNota1 = parseInt(posicionNota1) +  parseInt(staffExtraArray[1].nextSibling.getAttribute("width")); 
			document.getElementById("flecha").style.left = parseInt(screenWidth + posicionNota1 - 15) + "px";
		}catch(e){
			document.getElementById("flecha").style.left = parseInt(screenWidth + 25) + "px"; //25 a ojo
			// document.getElementById("flecha").style.left = parseInt(staffExtraArray[0].nextSibling.getAttribute("width") + "px"; //25 a ojo
		}

